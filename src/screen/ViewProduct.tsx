import {View, Text} from 'react-native';
import React from 'react';
import {Props} from '../constant/Interface';

export default function ViewProduct({navigation}: Props) {
  return (
    <View>
      <Text>ViewProduct</Text>
    </View>
  );
}
